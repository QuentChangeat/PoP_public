PoP Example

1) Run on the Example WASP-121b HST phase curve by opening the Jupyter notebook: pop_tutorial.ipynb

2) Try the command line mode with the following command:

pop -i params_W121.par -o output.h5 -p ../profiles/HSTWhitePhase_profile.py